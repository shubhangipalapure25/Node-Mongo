<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Chat</title>
</head>
<body>
   <h3>Edit Chat</h3>
   <p>Edit your chat : <%= chat._id %></p> 
   <p>This chat was send from <b><%= chat.from %></b> to <b> <%= chat.to %></b></p>
   <form method="POST" action="/chats/<%= chat._id %>?_method=PATCH">
    <div class="msg">
        <textarea placeholder="write Message" name="msg"><%= chat.msg %></textarea>
    </div>
    <button>Send</button>
   </form>
</body>
</html>